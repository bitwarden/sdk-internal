# Test vectors

Committed, fixed bitwarden accounts.

Checked-in fixtures, loaded by [`bitwarden-test-vector`](../crates/bitwarden-test-vector) and by the
WASM integration tests. The generator that produced them has been removed.
