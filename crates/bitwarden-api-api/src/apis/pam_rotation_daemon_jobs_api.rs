/*
 * Bitwarden Internal API
 *
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: latest
 * 
 * Generated by: https://openapi-generator.tech
 */


use async_trait::async_trait;
#[cfg(feature = "mockall")]
use mockall::automock;
use reqwest;
use std::sync::Arc;
use serde::{Deserialize, Serialize, de::Error as _};
use crate::{apis::{ResponseContent, AuthRequired}, models};
use super::{Error, configuration};
use crate::apis::ContentType;

#[cfg_attr(feature = "mockall", automock)]
#[cfg_attr(target_arch = "wasm32", async_trait(?Send))]
#[cfg_attr(not(target_arch = "wasm32"), async_trait)]
pub trait PamRotationDaemonJobsApi: Send + Sync {

    /// GET /rotation/daemon/jobs
    /// A daemon's currently claimable jobs on its assigned targets -- the poll. Doubles as a heartbeat when idle. Heartbeat contract: a daemon MUST call some daemon-facing rotation endpoint at an interval shorter than DaemonOfflineAfter for as long as it holds a claim, or the release sweep may reclaim the job once the claim's lease also expires; a daemon SHOULD poll no more often than HeartbeatMinInterval, since the heartbeat write is conditional on that interval and polling faster gains nothing.
    async fn get_all(&self, ) -> Result<models::ClaimableRotationJobResponseModelListResponseModel, Error>;
}

pub struct PamRotationDaemonJobsApiClient {
    configuration: Arc<configuration::Configuration>
}

impl PamRotationDaemonJobsApiClient {
    pub fn new(configuration: Arc<configuration::Configuration>) -> Self {
        Self { configuration }
    }
}



#[cfg_attr(target_arch = "wasm32", async_trait(?Send))]
#[cfg_attr(not(target_arch = "wasm32"), async_trait)]
impl PamRotationDaemonJobsApi for PamRotationDaemonJobsApiClient {

    /// A daemon's currently claimable jobs on its assigned targets -- the poll. Doubles as a heartbeat when idle. Heartbeat contract: a daemon MUST call some daemon-facing rotation endpoint at an interval shorter than DaemonOfflineAfter for as long as it holds a claim, or the release sweep may reclaim the job once the claim's lease also expires; a daemon SHOULD poll no more often than HeartbeatMinInterval, since the heartbeat write is conditional on that interval and polling faster gains nothing.
    async fn get_all(&self, ) -> Result<models::ClaimableRotationJobResponseModelListResponseModel, Error> {
        let local_var_configuration = &self.configuration;

        let local_var_client = &local_var_configuration.client;

        let local_var_uri_str = format!("{}/rotation/daemon/jobs", local_var_configuration.base_path);
        let mut local_var_req_builder = local_var_client.request(reqwest::Method::GET, local_var_uri_str.as_str());

        local_var_req_builder = local_var_req_builder.with_extension(AuthRequired::Bearer);

        bitwarden_api_base::process_with_json_response(local_var_req_builder).await
    }
}
