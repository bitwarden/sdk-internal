/*
 * Bitwarden Internal API
 *
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: latest
 *
 * Generated by: https://openapi-generator.tech
 */

use std::sync::Arc;

use async_trait::async_trait;
#[cfg(feature = "mockall")]
use mockall::automock;
use reqwest;
use serde::{Deserialize, Serialize, de::Error as _};

use super::{Error, configuration};
use crate::{
    apis::{AuthRequired, ContentType, ResponseContent},
    models,
};

#[cfg_attr(feature = "mockall", automock)]
#[cfg_attr(target_arch = "wasm32", async_trait(?Send))]
#[cfg_attr(not(target_arch = "wasm32"), async_trait)]
pub trait PamAccessConnectorRotationAttemptsApi: Send + Sync {
    /// POST /access-connectors/rotation/attempts/{id}/failure
    /// Reports a failed rotation attempt (spec RecordRotationFailed). Never forward raw
    /// target-system error output as ErrorCode/Detail -- it can echo credentials. Send a bounded
    /// error code plus an optional short detail instead. Either field exceeding its documented
    /// length is rejected as a 400, so cap them before sending -- a rejected report leaves the
    /// failure unrecorded until the job times out. The single reason the two combine into is
    /// truncated rather than rejected.
    async fn failure<'a>(
        &self,
        id: uuid::Uuid,
        report_rotation_failed_request_model: models::ReportRotationFailedRequestModel,
    ) -> Result<(), Error>;

    /// GET /access-connectors/rotation/attempts/{id}/cipher
    /// Returns the cipher for this access connector's claimed, executing attempt only -- never a
    /// general cipher read. Data is returned exactly as stored: opaque ciphertext the server never
    /// decrypts.
    async fn get_cipher<'a>(
        &self,
        id: uuid::Uuid,
    ) -> Result<models::RotationCipherResponseModel, Error>;

    /// PUT /access-connectors/rotation/attempts/{id}/cipher
    /// Writes the rotated secret back to the cipher (spec AcceptCipherUpdate) via an atomic
    /// capability check. 409 means the claim/attempt no longer holds, or LastKnownRevisionDate no
    /// longer matches the cipher's current revision (a concurrent user edit won) -- audited as
    /// write_rejected. 404 means the attempt id is unknown (no audit).
    async fn put_cipher<'a>(
        &self,
        id: uuid::Uuid,
        submit_cipher_update_request_model: models::SubmitCipherUpdateRequestModel,
    ) -> Result<(), Error>;

    /// POST /access-connectors/rotation/attempts/{id}/success
    /// Reports a successful rotation (spec RecordRotationSucceeded). Requires the attempt to
    /// already have a written cipher (the VerifiedBeforeSuccess backstop); otherwise the report is
    /// treated as stale (409, audited as report_rejected).
    async fn success<'a>(
        &self,
        id: uuid::Uuid,
        report_rotation_succeeded_request_model: models::ReportRotationSucceededRequestModel,
    ) -> Result<(), Error>;
}

pub struct PamAccessConnectorRotationAttemptsApiClient {
    configuration: Arc<configuration::Configuration>,
}

impl PamAccessConnectorRotationAttemptsApiClient {
    pub fn new(configuration: Arc<configuration::Configuration>) -> Self {
        Self { configuration }
    }
}

#[cfg_attr(target_arch = "wasm32", async_trait(?Send))]
#[cfg_attr(not(target_arch = "wasm32"), async_trait)]
impl PamAccessConnectorRotationAttemptsApi for PamAccessConnectorRotationAttemptsApiClient {
    /// Reports a failed rotation attempt (spec RecordRotationFailed). Never forward raw
    /// target-system error output as ErrorCode/Detail -- it can echo credentials. Send a bounded
    /// error code plus an optional short detail instead. Either field exceeding its documented
    /// length is rejected as a 400, so cap them before sending -- a rejected report leaves the
    /// failure unrecorded until the job times out. The single reason the two combine into is
    /// truncated rather than rejected.
    async fn failure<'a>(
        &self,
        id: uuid::Uuid,
        report_rotation_failed_request_model: models::ReportRotationFailedRequestModel,
    ) -> Result<(), Error> {
        let local_var_configuration = &self.configuration;

        let local_var_client = &local_var_configuration.client;

        let local_var_uri_str = format!(
            "{}/access-connectors/rotation/attempts/{id}/failure",
            local_var_configuration.base_path,
            id = id
        );
        let mut local_var_req_builder =
            local_var_client.request(reqwest::Method::POST, local_var_uri_str.as_str());

        local_var_req_builder = local_var_req_builder.with_extension(AuthRequired::Bearer);
        local_var_req_builder = local_var_req_builder.json(&report_rotation_failed_request_model);

        bitwarden_api_base::process_with_empty_response(local_var_req_builder).await
    }

    /// Returns the cipher for this access connector's claimed, executing attempt only -- never a
    /// general cipher read. Data is returned exactly as stored: opaque ciphertext the server never
    /// decrypts.
    async fn get_cipher<'a>(
        &self,
        id: uuid::Uuid,
    ) -> Result<models::RotationCipherResponseModel, Error> {
        let local_var_configuration = &self.configuration;

        let local_var_client = &local_var_configuration.client;

        let local_var_uri_str = format!(
            "{}/access-connectors/rotation/attempts/{id}/cipher",
            local_var_configuration.base_path,
            id = id
        );
        let mut local_var_req_builder =
            local_var_client.request(reqwest::Method::GET, local_var_uri_str.as_str());

        local_var_req_builder = local_var_req_builder.with_extension(AuthRequired::Bearer);

        bitwarden_api_base::process_with_json_response(local_var_req_builder).await
    }

    /// Writes the rotated secret back to the cipher (spec AcceptCipherUpdate) via an atomic
    /// capability check. 409 means the claim/attempt no longer holds, or LastKnownRevisionDate no
    /// longer matches the cipher's current revision (a concurrent user edit won) -- audited as
    /// write_rejected. 404 means the attempt id is unknown (no audit).
    async fn put_cipher<'a>(
        &self,
        id: uuid::Uuid,
        submit_cipher_update_request_model: models::SubmitCipherUpdateRequestModel,
    ) -> Result<(), Error> {
        let local_var_configuration = &self.configuration;

        let local_var_client = &local_var_configuration.client;

        let local_var_uri_str = format!(
            "{}/access-connectors/rotation/attempts/{id}/cipher",
            local_var_configuration.base_path,
            id = id
        );
        let mut local_var_req_builder =
            local_var_client.request(reqwest::Method::PUT, local_var_uri_str.as_str());

        local_var_req_builder = local_var_req_builder.with_extension(AuthRequired::Bearer);
        local_var_req_builder = local_var_req_builder.json(&submit_cipher_update_request_model);

        bitwarden_api_base::process_with_empty_response(local_var_req_builder).await
    }

    /// Reports a successful rotation (spec RecordRotationSucceeded). Requires the attempt to
    /// already have a written cipher (the VerifiedBeforeSuccess backstop); otherwise the report is
    /// treated as stale (409, audited as report_rejected).
    async fn success<'a>(
        &self,
        id: uuid::Uuid,
        report_rotation_succeeded_request_model: models::ReportRotationSucceededRequestModel,
    ) -> Result<(), Error> {
        let local_var_configuration = &self.configuration;

        let local_var_client = &local_var_configuration.client;

        let local_var_uri_str = format!(
            "{}/access-connectors/rotation/attempts/{id}/success",
            local_var_configuration.base_path,
            id = id
        );
        let mut local_var_req_builder =
            local_var_client.request(reqwest::Method::POST, local_var_uri_str.as_str());

        local_var_req_builder = local_var_req_builder.with_extension(AuthRequired::Bearer);
        local_var_req_builder =
            local_var_req_builder.json(&report_rotation_succeeded_request_model);

        bitwarden_api_base::process_with_empty_response(local_var_req_builder).await
    }
}
