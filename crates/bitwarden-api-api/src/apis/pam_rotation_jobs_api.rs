/*
 * Bitwarden Internal API
 *
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: latest
 * 
 * Generated by: https://openapi-generator.tech
 */


use async_trait::async_trait;
#[cfg(feature = "mockall")]
use mockall::automock;
use reqwest;
use std::sync::Arc;
use serde::{Deserialize, Serialize, de::Error as _};
use crate::{apis::{ResponseContent, AuthRequired}, models};
use super::{Error, configuration};
use crate::apis::ContentType;

#[cfg_attr(feature = "mockall", automock)]
#[cfg_attr(target_arch = "wasm32", async_trait(?Send))]
#[cfg_attr(not(target_arch = "wasm32"), async_trait)]
pub trait PamRotationJobsApi: Send + Sync {

    /// POST /rotation/jobs/{id}/claim
    /// Atomically claims a job -- first-claim-wins -- and returns the work snapshot needed to execute the rotation. 409 means another daemon won the race (claim a different job); 404 means this daemon was never eligible to claim it (no assignment, wrong organization, or a disabled target/config).
    async fn claim<'a>(&self, id: uuid::Uuid) -> Result<models::RotationClaimResponseModel, Error>;
}

pub struct PamRotationJobsApiClient {
    configuration: Arc<configuration::Configuration>
}

impl PamRotationJobsApiClient {
    pub fn new(configuration: Arc<configuration::Configuration>) -> Self {
        Self { configuration }
    }
}



#[cfg_attr(target_arch = "wasm32", async_trait(?Send))]
#[cfg_attr(not(target_arch = "wasm32"), async_trait)]
impl PamRotationJobsApi for PamRotationJobsApiClient {

    /// Atomically claims a job -- first-claim-wins -- and returns the work snapshot needed to execute the rotation. 409 means another daemon won the race (claim a different job); 404 means this daemon was never eligible to claim it (no assignment, wrong organization, or a disabled target/config).
    async fn claim<'a>(&self, id: uuid::Uuid) -> Result<models::RotationClaimResponseModel, Error> {
        let local_var_configuration = &self.configuration;

        let local_var_client = &local_var_configuration.client;

        let local_var_uri_str = format!("{}/rotation/jobs/{id}/claim", local_var_configuration.base_path, id=id);
        let mut local_var_req_builder = local_var_client.request(reqwest::Method::POST, local_var_uri_str.as_str());

        local_var_req_builder = local_var_req_builder.with_extension(AuthRequired::Bearer);

        bitwarden_api_base::process_with_json_response(local_var_req_builder).await
    }
}
