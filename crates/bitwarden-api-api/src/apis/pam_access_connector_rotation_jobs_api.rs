/*
 * Bitwarden Internal API
 *
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: latest
 *
 * Generated by: https://openapi-generator.tech
 */

use std::sync::Arc;

use async_trait::async_trait;
#[cfg(feature = "mockall")]
use mockall::automock;
use reqwest;
use serde::{Deserialize, Serialize, de::Error as _};

use super::{Error, configuration};
use crate::{
    apis::{AuthRequired, ContentType, ResponseContent},
    models,
};

#[cfg_attr(feature = "mockall", automock)]
#[cfg_attr(target_arch = "wasm32", async_trait(?Send))]
#[cfg_attr(not(target_arch = "wasm32"), async_trait)]
pub trait PamAccessConnectorRotationJobsApi: Send + Sync {
    /// POST /access-connectors/rotation/jobs/{id}/claim
    /// Atomically claims a job -- first-claim-wins -- and returns the work snapshot needed to
    /// execute the rotation. 409 means another access connector won the race (claim a different
    /// job); 404 means this access connector was never eligible to claim it (no assignment, wrong
    /// organization, or a disabled target/config).
    async fn claim<'a>(&self, id: uuid::Uuid) -> Result<models::RotationClaimResponseModel, Error>;

    /// GET /access-connectors/rotation/jobs
    /// The calling access connector's currently claimable jobs on its assigned targets -- the poll.
    /// Doubles as a heartbeat when idle. Heartbeat contract: an access connector MUST call some
    /// connector-facing rotation endpoint at an interval shorter than ConnectorOfflineAfter for as
    /// long as it holds a claim, or the release sweep may reclaim the job once the claim's lease
    /// also expires; an access connector SHOULD poll no more often than HeartbeatMinInterval, since
    /// the heartbeat write is conditional on that interval and polling faster gains nothing.
    async fn get_all(
        &self,
    ) -> Result<models::ClaimableRotationJobResponseModelListResponseModel, Error>;
}

pub struct PamAccessConnectorRotationJobsApiClient {
    configuration: Arc<configuration::Configuration>,
}

impl PamAccessConnectorRotationJobsApiClient {
    pub fn new(configuration: Arc<configuration::Configuration>) -> Self {
        Self { configuration }
    }
}

#[cfg_attr(target_arch = "wasm32", async_trait(?Send))]
#[cfg_attr(not(target_arch = "wasm32"), async_trait)]
impl PamAccessConnectorRotationJobsApi for PamAccessConnectorRotationJobsApiClient {
    /// Atomically claims a job -- first-claim-wins -- and returns the work snapshot needed to
    /// execute the rotation. 409 means another access connector won the race (claim a different
    /// job); 404 means this access connector was never eligible to claim it (no assignment, wrong
    /// organization, or a disabled target/config).
    async fn claim<'a>(&self, id: uuid::Uuid) -> Result<models::RotationClaimResponseModel, Error> {
        let local_var_configuration = &self.configuration;

        let local_var_client = &local_var_configuration.client;

        let local_var_uri_str = format!(
            "{}/access-connectors/rotation/jobs/{id}/claim",
            local_var_configuration.base_path,
            id = id
        );
        let mut local_var_req_builder =
            local_var_client.request(reqwest::Method::POST, local_var_uri_str.as_str());

        local_var_req_builder = local_var_req_builder.with_extension(AuthRequired::Bearer);

        bitwarden_api_base::process_with_json_response(local_var_req_builder).await
    }

    /// The calling access connector's currently claimable jobs on its assigned targets -- the poll.
    /// Doubles as a heartbeat when idle. Heartbeat contract: an access connector MUST call some
    /// connector-facing rotation endpoint at an interval shorter than ConnectorOfflineAfter for as
    /// long as it holds a claim, or the release sweep may reclaim the job once the claim's lease
    /// also expires; an access connector SHOULD poll no more often than HeartbeatMinInterval, since
    /// the heartbeat write is conditional on that interval and polling faster gains nothing.
    async fn get_all(
        &self,
    ) -> Result<models::ClaimableRotationJobResponseModelListResponseModel, Error> {
        let local_var_configuration = &self.configuration;

        let local_var_client = &local_var_configuration.client;

        let local_var_uri_str = format!(
            "{}/access-connectors/rotation/jobs",
            local_var_configuration.base_path
        );
        let mut local_var_req_builder =
            local_var_client.request(reqwest::Method::GET, local_var_uri_str.as_str());

        local_var_req_builder = local_var_req_builder.with_extension(AuthRequired::Bearer);

        bitwarden_api_base::process_with_json_response(local_var_req_builder).await
    }
}
