/*
 * Bitwarden Internal API
 *
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: latest
 *
 * Generated by: https://openapi-generator.tech
 */

use std::sync::Arc;

use async_trait::async_trait;
#[cfg(feature = "mockall")]
use mockall::automock;
use reqwest;
use serde::{Deserialize, Serialize, de::Error as _};

use super::{Error, configuration};
use crate::{
    apis::{AuthRequired, ContentType, ResponseContent},
    models,
};

#[cfg_attr(feature = "mockall", automock)]
#[cfg_attr(target_arch = "wasm32", async_trait(?Send))]
#[cfg_attr(not(target_arch = "wasm32"), async_trait)]
pub trait PamAccessConnectorsApi: Send + Sync {
    /// POST /organizations/{orgId}/access-connectors/{id}/assignments
    async fn assign_target<'a>(
        &self,
        org_id: uuid::Uuid,
        id: uuid::Uuid,
        assign_access_connector_target_request_model: models::AssignAccessConnectorTargetRequestModel,
    ) -> Result<(), Error>;

    /// DELETE /organizations/{orgId}/access-connectors/{id}
    /// Permanently deletes an access connector and invalidates its credential. The access connector
    /// held the plaintext organization key -- rotating the organization key is the remediation for
    /// a suspected compromise.
    async fn delete<'a>(&self, org_id: uuid::Uuid, id: uuid::Uuid) -> Result<(), Error>;

    /// POST /organizations/{orgId}/access-connectors/{id}/disable
    /// Disables an access connector (reversible): it stops claiming new jobs and its running jobs
    /// are released, but its credential is retained so it can be re-enabled later.
    async fn disable<'a>(&self, org_id: uuid::Uuid, id: uuid::Uuid) -> Result<(), Error>;

    /// POST /organizations/{orgId}/access-connectors/{id}/enable
    /// Re-enables a disabled access connector so it can authenticate and claim jobs again.
    async fn enable<'a>(&self, org_id: uuid::Uuid, id: uuid::Uuid) -> Result<(), Error>;

    /// GET /organizations/{orgId}/access-connectors/{id}
    /// Returns one access connector with its recent rotation activity -- the jobs it has worked and
    /// the attempts it recorded against them, newest first.
    async fn get<'a>(
        &self,
        org_id: uuid::Uuid,
        id: uuid::Uuid,
    ) -> Result<models::PamAccessConnectorDetailResponseModel, Error>;

    /// GET /organizations/{orgId}/access-connectors
    async fn get_all<'a>(
        &self,
        org_id: uuid::Uuid,
    ) -> Result<models::PamAccessConnectorResponseModelListResponseModel, Error>;

    /// POST /organizations/{orgId}/access-connectors
    /// Registers an access connector and returns its client secret. The secret is shown exactly
    /// once here -- the server hashes it for storage and cannot return it again.
    async fn post<'a>(
        &self,
        org_id: uuid::Uuid,
        register_access_connector_request_model: models::RegisterAccessConnectorRequestModel,
    ) -> Result<models::RegisterAccessConnectorResponseModel, Error>;

    /// DELETE /organizations/{orgId}/access-connectors/{id}/assignments/{targetSystemId}
    async fn unassign_target<'a>(
        &self,
        org_id: uuid::Uuid,
        id: uuid::Uuid,
        target_system_id: uuid::Uuid,
    ) -> Result<(), Error>;
}

pub struct PamAccessConnectorsApiClient {
    configuration: Arc<configuration::Configuration>,
}

impl PamAccessConnectorsApiClient {
    pub fn new(configuration: Arc<configuration::Configuration>) -> Self {
        Self { configuration }
    }
}

#[cfg_attr(target_arch = "wasm32", async_trait(?Send))]
#[cfg_attr(not(target_arch = "wasm32"), async_trait)]
impl PamAccessConnectorsApi for PamAccessConnectorsApiClient {
    async fn assign_target<'a>(
        &self,
        org_id: uuid::Uuid,
        id: uuid::Uuid,
        assign_access_connector_target_request_model: models::AssignAccessConnectorTargetRequestModel,
    ) -> Result<(), Error> {
        let local_var_configuration = &self.configuration;

        let local_var_client = &local_var_configuration.client;

        let local_var_uri_str = format!(
            "{}/organizations/{orgId}/access-connectors/{id}/assignments",
            local_var_configuration.base_path,
            orgId = org_id,
            id = id
        );
        let mut local_var_req_builder =
            local_var_client.request(reqwest::Method::POST, local_var_uri_str.as_str());

        local_var_req_builder = local_var_req_builder.with_extension(AuthRequired::Bearer);
        local_var_req_builder =
            local_var_req_builder.json(&assign_access_connector_target_request_model);

        bitwarden_api_base::process_with_empty_response(local_var_req_builder).await
    }

    /// Permanently deletes an access connector and invalidates its credential. The access connector
    /// held the plaintext organization key -- rotating the organization key is the remediation for
    /// a suspected compromise.
    async fn delete<'a>(&self, org_id: uuid::Uuid, id: uuid::Uuid) -> Result<(), Error> {
        let local_var_configuration = &self.configuration;

        let local_var_client = &local_var_configuration.client;

        let local_var_uri_str = format!(
            "{}/organizations/{orgId}/access-connectors/{id}",
            local_var_configuration.base_path,
            orgId = org_id,
            id = id
        );
        let mut local_var_req_builder =
            local_var_client.request(reqwest::Method::DELETE, local_var_uri_str.as_str());

        local_var_req_builder = local_var_req_builder.with_extension(AuthRequired::Bearer);

        bitwarden_api_base::process_with_empty_response(local_var_req_builder).await
    }

    /// Disables an access connector (reversible): it stops claiming new jobs and its running jobs
    /// are released, but its credential is retained so it can be re-enabled later.
    async fn disable<'a>(&self, org_id: uuid::Uuid, id: uuid::Uuid) -> Result<(), Error> {
        let local_var_configuration = &self.configuration;

        let local_var_client = &local_var_configuration.client;

        let local_var_uri_str = format!(
            "{}/organizations/{orgId}/access-connectors/{id}/disable",
            local_var_configuration.base_path,
            orgId = org_id,
            id = id
        );
        let mut local_var_req_builder =
            local_var_client.request(reqwest::Method::POST, local_var_uri_str.as_str());

        local_var_req_builder = local_var_req_builder.with_extension(AuthRequired::Bearer);

        bitwarden_api_base::process_with_empty_response(local_var_req_builder).await
    }

    /// Re-enables a disabled access connector so it can authenticate and claim jobs again.
    async fn enable<'a>(&self, org_id: uuid::Uuid, id: uuid::Uuid) -> Result<(), Error> {
        let local_var_configuration = &self.configuration;

        let local_var_client = &local_var_configuration.client;

        let local_var_uri_str = format!(
            "{}/organizations/{orgId}/access-connectors/{id}/enable",
            local_var_configuration.base_path,
            orgId = org_id,
            id = id
        );
        let mut local_var_req_builder =
            local_var_client.request(reqwest::Method::POST, local_var_uri_str.as_str());

        local_var_req_builder = local_var_req_builder.with_extension(AuthRequired::Bearer);

        bitwarden_api_base::process_with_empty_response(local_var_req_builder).await
    }

    /// Returns one access connector with its recent rotation activity -- the jobs it has worked and
    /// the attempts it recorded against them, newest first.
    async fn get<'a>(
        &self,
        org_id: uuid::Uuid,
        id: uuid::Uuid,
    ) -> Result<models::PamAccessConnectorDetailResponseModel, Error> {
        let local_var_configuration = &self.configuration;

        let local_var_client = &local_var_configuration.client;

        let local_var_uri_str = format!(
            "{}/organizations/{orgId}/access-connectors/{id}",
            local_var_configuration.base_path,
            orgId = org_id,
            id = id
        );
        let mut local_var_req_builder =
            local_var_client.request(reqwest::Method::GET, local_var_uri_str.as_str());

        local_var_req_builder = local_var_req_builder.with_extension(AuthRequired::Bearer);

        bitwarden_api_base::process_with_json_response(local_var_req_builder).await
    }

    async fn get_all<'a>(
        &self,
        org_id: uuid::Uuid,
    ) -> Result<models::PamAccessConnectorResponseModelListResponseModel, Error> {
        let local_var_configuration = &self.configuration;

        let local_var_client = &local_var_configuration.client;

        let local_var_uri_str = format!(
            "{}/organizations/{orgId}/access-connectors",
            local_var_configuration.base_path,
            orgId = org_id
        );
        let mut local_var_req_builder =
            local_var_client.request(reqwest::Method::GET, local_var_uri_str.as_str());

        local_var_req_builder = local_var_req_builder.with_extension(AuthRequired::Bearer);

        bitwarden_api_base::process_with_json_response(local_var_req_builder).await
    }

    /// Registers an access connector and returns its client secret. The secret is shown exactly
    /// once here -- the server hashes it for storage and cannot return it again.
    async fn post<'a>(
        &self,
        org_id: uuid::Uuid,
        register_access_connector_request_model: models::RegisterAccessConnectorRequestModel,
    ) -> Result<models::RegisterAccessConnectorResponseModel, Error> {
        let local_var_configuration = &self.configuration;

        let local_var_client = &local_var_configuration.client;

        let local_var_uri_str = format!(
            "{}/organizations/{orgId}/access-connectors",
            local_var_configuration.base_path,
            orgId = org_id
        );
        let mut local_var_req_builder =
            local_var_client.request(reqwest::Method::POST, local_var_uri_str.as_str());

        local_var_req_builder = local_var_req_builder.with_extension(AuthRequired::Bearer);
        local_var_req_builder =
            local_var_req_builder.json(&register_access_connector_request_model);

        bitwarden_api_base::process_with_json_response(local_var_req_builder).await
    }

    async fn unassign_target<'a>(
        &self,
        org_id: uuid::Uuid,
        id: uuid::Uuid,
        target_system_id: uuid::Uuid,
    ) -> Result<(), Error> {
        let local_var_configuration = &self.configuration;

        let local_var_client = &local_var_configuration.client;

        let local_var_uri_str = format!(
            "{}/organizations/{orgId}/access-connectors/{id}/assignments/{targetSystemId}",
            local_var_configuration.base_path,
            orgId = org_id,
            id = id,
            targetSystemId = target_system_id
        );
        let mut local_var_req_builder =
            local_var_client.request(reqwest::Method::DELETE, local_var_uri_str.as_str());

        local_var_req_builder = local_var_req_builder.with_extension(AuthRequired::Bearer);

        bitwarden_api_base::process_with_empty_response(local_var_req_builder).await
    }
}
