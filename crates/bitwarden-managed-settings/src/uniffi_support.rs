//! Foreign-type re-exports for UniFFI consumers of this crate.
//!
//! Nothing here yet — the crate's public types are all defined locally and
//! contain only primitives. Kept as a stub so the layout mirrors
//! `bitwarden-policies` and future shared types can be wired in easily.
